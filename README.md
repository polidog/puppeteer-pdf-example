# Puppetter PDF and Screenshot Example by Cloud Functions for Firebase 

```
$ git clone https://github.com/polidog/puppeteer-pdf-example/
$ cd puppeteer-pdf-example/functions 
$ npm install
$ fireabase serve --project <your project id>
```